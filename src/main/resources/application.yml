spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/TCG-Pokermon
    username: postgres
    password: kauan123

    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  application:
    name: pokermon
